# CodeGuardian

Multi-agent code review. A supervisor classifies the files in a PR and fans
work out to specialist agents; deterministic tools find the issues, an
optional LLM layer explains and ranks them.

Early days — security agent first, more specialists to follow.

```bash
pip install -r requirements.txt
python -m pytest
```
