# Sample: clean code

A small, well-behaved module used to demonstrate a high CodeGuardian score:
documented, tested, no secrets, no complexity red flags.

```bash
python -m app.harness sample_good_code
```
